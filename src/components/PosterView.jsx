function PosterView({ requests, addRequest, updateStatus }) {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold">Poster View</h2>
      <p className="text-gray-500 text-sm">Stub — {requests.length} request(s)</p>
    </div>
  )
}

export default PosterView
