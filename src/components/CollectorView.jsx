function CollectorView({ requests, updateStatus }) {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold">Collector View</h2>
      <p className="text-gray-500 text-sm">Stub — {requests.length} request(s)</p>
    </div>
  )
}

export default CollectorView
